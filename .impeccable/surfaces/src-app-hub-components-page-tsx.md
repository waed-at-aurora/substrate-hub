---
version: 1
slug: "src-app-hub-components-page-tsx"
primary_target: "src/app/(hub)/components/page.tsx"
related_targets: ["src/components/catalog.tsx","src/components/islands.tsx","src/app/globals.css"]
---

# Scope and mode
Route: `/components`. Mode: Read with an Operate edge; visitors browse real UI and interact with it before opening detailed documentation.

# Audience and job
Aurora product designers and engineers need to recognize what Substrate ships, compare component families visually, and move to the exact Storybook entry when they need API detail.

# Primary action and proof
Primary action: explore live specimens; secondary action: open Storybook. Proof is the actual `@aurora-ui/components-v2` render, including real states and interactions.

# Direction and memorable moment
A code-free live specimen wall inside the established night drafting world. A dense 12-column ruled gallery mixes hero, wide, and compact footprints so the page reads like a working component room rather than an inventory table. The composite selection card opens the wall at demonstration scale.

# Constraints
No code snippets or source-history table. The hub may frame components but must not restyle their internal package grammar. Keep search, layer filters, responsive single-column adaptation, direct Storybook links, and an explicit empty-state reset.
